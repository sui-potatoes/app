# Commander

Implements Commander frontend as a desktop / mobile application. Uses [`macroquad`](https://macroquad.rs/) as the engine for rendering.

## Build & Run

From anywhere:

```bash
cargo run --release
```

## Environment Setting

Requires environment variables as shown in [the template](./.env.template).

## License

MIT
