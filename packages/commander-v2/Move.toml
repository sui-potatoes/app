[package]
name = "Commander"
edition = "2024.beta"
license = "MIT"
authors = ["Damir Shamanaev (damirka.ru@gmail.com)"]

[dependencies]
bit-field = { local = "../bit-field" }
Codec = { local = "../codec" }
grid = { local = "../grid" }
NameGenerator = { local = "../name-gen" }

[addresses]
commander = "0x0"
